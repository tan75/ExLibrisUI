const initialState = {
  books: [
    {
      title: 'Harry Potter 5',
      pages: 5,
    },
  ],
  inputValue: '',
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
